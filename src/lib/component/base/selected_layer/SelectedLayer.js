
class SelectedLayer {
    addLayer(layer, layerCapability) {}
}

export class SelectedLayerWMS {
    addLayer(layer, layerCapability) {
        sourceLayerWMS = a_layer
        layerCapabilityWMS = layerCapability
    }
}